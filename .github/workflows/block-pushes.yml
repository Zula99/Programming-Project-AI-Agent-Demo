# name: Block Direct Pushes to Main

# on:
#   push:
#     branches:
#       - main
#       - master

# jobs:
#   block-push:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check for direct push to protected branch
#         if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#         run: |
#           echo "This branch is protected. Direct pushes are not allowed."
#           echo "Please create a pull request to merge your changes."
#           exit 1

# Commented the file out for now as it is not working as intended.
